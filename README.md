# agent

售前的方案一般是如下路径：
﻿﻿﻿
大模型时代，随着模型能力的飙升，上述所有模块都逐渐出现了大模型的身影。本文以上述框架在大模型时代的变化，展示店小蜜业务中，我们是如何逐渐从被动的一问一答的对话系统，转向主动出击、主动寻求问题解决的agent系统的。
工具调用：主动出击
传统对话系统中，知识召回通常通过召回排序流程实现，作为下游的回复生成部分要么是商家已经写好的答案，要么是基于召回知识进行简单优化模型；其有一个非常大的特点：回复生成的模型并不能直接干涉知识召回的流程。如果类比人工客服的工作流的话，就像是人工客服只能基于一套固定的知识回答用户的问题，而没有办法在他认为无法回答问题的时候，主动出击寻找他想要的知识。
﻿
然后大模型Agent来了。大模型强大的语义理解能力使得我们可以将大模型当作半个人工客服，赋予其主动寻找有效知识的能力，即工具调用的能力。基于这个思想，我们设计了第一版的售前Agent框架：
﻿﻿﻿
这里遵循了OpenAI的Tool-Use协议，希望用一个模型直接生成答案，发现知识不足时则调用工具寻找有效知识。为了业务效果，我们希望这版Agent可以遵守人工客服的售前流程SOP，因此我们对模型的Prompt使用了类似“状态机”的方法

State Machine
在直接使用Function Call失败后，针对其“无法始终输出json”的问题，我们转而选择构建自己的协议，放弃function_call模板。针对“无法follow人工SOP”的问题，考虑使用状态机的解法。其prompt例子如下：
### 对话流程(以state machine形式呈现)
<state_machine_schema>
{
  "id": "<string, unique step identifier, human readable, like '1_init'>",
  "description": "<string, explanation of the step’s purpose>",
  "instructions": [
    // list of strings describing what the agent should do in  this state
  ],
  "examples": [
    // list of short example scripts or utterances
  ],
  "tool":"{
    "name": "<string, name of the tool>"
    "arguments":[
        // parameter list
    ]
    "output":[
        // key list of the return JSON object
    ]
  },
  "skill":"<The reply skill this state can use. When tool is not null, this must be '调用工具'>",
  "transitions": [
    {
      "next_step": "<string, the ID of the next step>",
      "condition": "<string, under what condition the step transitions>"
    }
    // more transitions can be added if needed
  ]
}
</state_machine_schema>
{
      "id": "需求总结",
      "description": "总结需求；路由节点",
      "instructions": [
        "结合用户与客服的对话上文理解用户当前问题，然后总结消费者当前需求",
        "根据不同condition路由到后续不同节点"
      ],
      "transitions": [
        {
          "next_step": "是否调用工具2",
          "condition": "需求不明确"
        },
        {
          "next_step": "是否调用工具3",
          "condition": "需求明确"
        }
      ]
},
{
      "id": "需求挖掘",
      "description": "用户需求不明确时尝试挖掘用户需求；执行节点",
      "instructions": [
        "请结合<猜你想问>工具的结果，通过追问、猜测等方式尝试了解消费者的真实需求。"
      ],
      "skill": "需求挖掘",
      "examples":[
        "工具返回了'羽绒服有手提袋吗？'，说明这个问题是其他消费者很爱问的问题，可以回复'亲亲，您可能感兴趣的问题：羽绒服有口袋吗？'"
      ]
}
所谓状态机，就是通过人工定义SOP中各个节点的转移和吸收条件，并且要求LLM在输出中包含状态转移流程的方法，来严格控制大模型的COT过程，从而让大模型的思路严格按照人工客服的SOP流转的一种方式。
{
  "状态路由":"初始化->需求总结->是否调用工具3->尺码表",
  "需求":"咨询尺码",
  "tool":"尺码表",
  "skill":"调用工具",
  "回复":""
}
只要在prompt里仿照人工客服的SOP定义清楚所有状态，并对不同的末尾节点定义不同的输出形式，即可实现类似Function Call的“选择调用工具或直接回复”功能。
通过状态机我们成功让大模型跟随了人工客服的SOP，并且可以自行选择调用工具或是直接回复。但是依然有多个问题需要解决：
●可迭代性差。基座模型在SOP转移细节上的理解与业务会有一定的gap，通常只能通过训练解决。但是在模型没有上线前，真实样本的收集较为困难。其本质原因是使用状态机表达的SOP与大模型原生的思考模式并不匹配，这种方式比较难发挥大模型的潜力。
●可维护性差。下图是状态机Prompt的拆解。对于商家的个性化要求或是SOP的更新，需要花大量精力修改适配Prompt，并且还需要通过离线测试集确保大模型对状态的理解与人工客服是一致的。

●优点：
○灵活性高，对于简单问题可以直接回复，复杂问题可以调用工具；
○以“工具调用”（数十种工具）取代了以往的“意图识别”（上千种场景），使得分类准确率更高。
●缺点：
○由于将状态机SOP嵌入，导致prompt极长，消耗资源过高；
○可维护性、可拓展性很差，既要调用工具又要回复的模式让所有模块耦合，不利于任务分解与迭代。

稳定性考量：拆分Planner和Responser
第一版Agent中将工具调用和回复全都由一个模型完成，虽然这非常符合agent的设计理念，但彼时基础模型（DeepSeek V3时期）的能力还没有达到一个非常强大的水平。当时，我们有以下几个观察：
❏不使用推理模型时，同时让一个模型执行两种任务（调用工具和回复），其准确率不会特别高，比不上写两套prompt后让模型分别执行两个任务；
❏使用推理模型时，不经训练的基础模型的RT（时延）对于店小蜜的线上业务是不可接受的。
基于业务的现实考量和以上两个制约条件，最终上线的版本选择了使用两个不同的模型进行工具调用和最终回复。
﻿﻿
拆分Planner和Responser﻿
●优点：
○目标导向：完成从SOP状态机到目标导向架构的转型，给予大模型更大的发挥空间，减少SOP不能考虑到的大量corner case，离线评测准确率达到91%；
○可维护性提升：解耦planner和responser后，两边模块可以分开迭代，提升合作效率。
●缺点：
○planner的思路直接给到critic时经常会与工具结果矛盾，导致回复不达预期；
○工程链路限制了工具只能调用一个，减少了复杂场景的可用性。
解锁更高上限：引入推理模型
解耦planner和responser的agent上线以后，相对于老的店小蜜机器人获得了超过约30%的转化率提升。但是由于使用了instruct版本（非推理版本）的planner和responser，其解决困难问题的能力依然不足。想要获得更强的解决问题的能力，我们需要在线上部署推理模型。而实现这一点在线上服务、推理资源、业务指标上有非常多的约束条件：
❏不经过训练的推理模型RT过长；
❏最大规模（如235B量级）的模型训练成本过高，以我们现有的资源基本只能支持到32B级别模型的后训练；最大规模的推理模型带来的线上RT也基本无法承受；
❏最大规模（如235B量级）的模型经实践，在业务指标上的表现要比小模型好得多。这是由于更大尺寸的模型能够记住更多的世界知识带来的优势。
对比模型
qwen3-235B-instruct
qwen3-32B-thinking
业务指标（转化率、转人工率）
更好✅
较低❌
困难case正确性
较低❌
更好✅
不同尺寸/推理模型的线上效果对比﻿
从上表可见，如果我们直接将responser从一个大的基座模型替换为小的推理模型，虽然能解决更多困难的case，给消费者带来更好的体验，但会损失业务指标。因此，有没有两全其美的方法呢？
﻿﻿
推理Reflection帮助agent更好总结知识
为了平衡业务指标和推理能力的trade-off，我们选择在responser之前再添加一个一个reflection模块（小的推理模型），负责将工具结果和有效知识经过推理模型总结成简短的文本，然后将其作为最后responser模型的输入。一句话总结reflection的作用：用较低的时间和资源成本帮助最大规模的模型进行思考。
prompt
## 角色说明
你是一个淘宝电商店铺的智能客服助理，你的任务是结合用户问题、参考知识、工具内容和客服规则，为后续的“答复客服”筛选知识，选择需要输出的卡片，总结注意事项，辅助答复客服更好的回答用户的问题，促进用户的下单。

## 核心概念以及规则
<核心概念定义>
|概念名称|解释|
|----------|------------------------------------------------------------|
|商家知识库|商家配置于商品上的相关知识|
|API|查询得到当前商品或用户的相关信息|
...
</核心概念定义>

<智能客服回复规则>
|规则名称|描述|
|----------------|------------------------------------------------------------|
|禁止虚构信息|禁止回复中包含参考知识中**不存在的内容**，包括虚构活动、图片、链接等。|
...
</智能客服回复规则>

## 任务要求
1. 根据用户最后的问题，用简练的语言筛选出所有参考知识中与用户问题相关的知识。注意：只需要给出知识，不需要给出结论性的回复。如果所有参考信息均对回答问题没有帮助，输出“无，需拒识”。
2. 根据用户最后的问题，用简练的语言在所有的回复规则中筛选出和回复用户问题最相关的注意事项。
3. 当卡片信息和用户问题不是完全无关的时候，尽量选择多种类的卡片。如果输出多张卡片，在输出时用"+"号连接。如果工具内容中没有卡片、或你认为不需要输出卡片，输出“无”
4. 需要以JSON的格式进行输出。

## json_schema
{"type":"object","properties":{
"有效知识":{"type":"string","description":"总结对于“答复客服”回复最重要的知识，最多二条，不超过40字。仅客观知识，非回复"},
"注意事项:{"type":"string","description":"总结对于“答复客服”回复最重要的注意事项，最多二条，不超过40字"},
"输出卡片":{"type":"string"}
},"required":["有效知识","注意事项","输出卡片"]}

## 商品信息
店铺名称：xx旗舰店
已获取到商品信息：
<所咨询商品的信息>
商品ID：7425736xxx
商品链接：https://item.taobao.com/item.htm?id=7425736xxx
商品名称：【重磅产品】xxxx
商品类目：智能家居系统
商品品类：电子/电工
<详情页属性>
| 属性名 | 属性值 | 属性名解释 | 属性值解释 |
| ---- | ---- | ---- | ---- |
| 品牌 | xxx | 品牌是指由企业或个人用于区分其产品或服务，并通过这些产品或服务传达特定品质、价值或形象的一系列标识、名称、符号或设计。 | |
...
</详情页属性>
<商品SKU>
| 序号 | 库存 | 颜色分类 |
| ---- | ---- | ---- |
| SKU1 | 有货 | 深空灰色 |
| SKU2 | 有货 | 白色 |
</商品SKU>
<当前商品知识>
| 问题 | 答案 |
|----|----|
| 智能开关和智能灯如何搭配使用 | 智能灯建议搭配凌动开关使用的哦，我们开关是没有凌动功能的哈。xxx |
...
</当前商品知识>
</所咨询商品的信息>
<当前时刻>2025-12-16 14:17:33</当前时刻>

## 当前对话
<用户><卡片消息>用户发送了一条商品链接：https://item.taobao.com/item.htm?id=7425736xxx
商品名称是：【重磅产品】xxxx
商品id是：7425736xxx</卡片消息>
</用户>
<客服>❤【重磅产品】xxxx❤是一款多功能的语音开关，xxx，
贴士：本产品仅限室内环境中使用。</客服>
<用户>现在还能实现智能开关吗
这个我全屋要弄</用户>

Planer的训练过程
店小蜜在FY26将原本的RAG框架升级成了Agent框架，整体框架发生了非常大的改变。特别的，在意图层面，由原先依赖“场景”、“正负例句”以及多个小模型的结构直接升级为了Planner架构。Planner负责在用户的每一个query进来后对用户的意图进行理解，给出回复思路，以及在必要时选择合适的工具。
﻿
